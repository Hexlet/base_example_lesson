items:
  unique_slug:
    test_file: tests/test_unique_slug.py
    name: Name of the task
    description: |-
      User's solution must be tested. This is why Makefile should include the 'test' task which includes all the test.
      The correct solution is placed in the same file (for simplicity) and wrapped in special BEGIN and END markers (see example).
      The code between those markers is not shown to users.

  index_of:
    test_file: tests/test_index_of.py
    name: Name of another task
    description: Description

  reverse:
    test_file: tests/test_reverse.py
    name: Name of yet another task
    description: Description
