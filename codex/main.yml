items:
  unique_slug:
    test_file: tests/test_unique_slug.py
    name: Имя задачи
    description: |-
      Код, который заливает преподаватель, должен быть протестирован.
      Поэтому в файле Makefile необходимо сделать задачу test, которая прогоняет все тесты.
      Решение задачи описывается в том же файле где, и тесты (для простоты создания урока).
      Это решение необходимо обернуть в специальный маркер (см. пример), благодаря которому solution вырезается и не показывается пользователям.

  index_of:
    test_file: tests/test_index_of.py
    name: Имя задачи
    description: Описание

  reverse:
    test_file: tests/test_reverse.py
    name: Имя задачи
    description: Описание
